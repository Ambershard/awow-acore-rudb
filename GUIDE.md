# Справочник макросов текста World of Warcraft

Клиент игры содержит встроенный парсер текста, который позволяет динамически подставлять имена, классы, учитывать пол персонажа, а также форматировать текст (цвета, ссылки, переносы). Эти макросы можно использовать в таблицах `quest_template`, `quest_request_items`, `quest_offer_reward`, `npc_text`, `page_text` и `mail`.

### 1. Базовые переменные персонажа (Имя, Класс, Раса)
*Примечание: Использование заглавной буквы (`$N`, `$C`, `$R`) должно делать слово с большой буквы, а строчной (`$n`, `$c`, `$r`) — с маленькой (однако в некоторых окнах диалогов клиент форсирует заглавную букву автоматически).*

*   **`$N`** или **`$n`** — Имя персонажа.
    *   *Пример:* `Привет, $N!` ➔ *Привет, Тралл!*
*   **`$C`** или **`$c`** — Класс персонажа (в именительном падеже).
    *   *Пример:* `Ты отличный $c.` ➔ *Ты отличный Воин.*
*   **`$R`** или **`$r`** — Раса персонажа.
    *   *Пример:* `Для $r это пара пустяков.` ➔ *Для Дворфа это пара пустяков.*

### 2. Гендерные макросы (Зависимость от пола)
Критически важно для русской локализации. Макрос выбирает слово в зависимости от пола персонажа, читающего текст.
**Синтаксис:** `$Gмужской_вариант:женский_вариант;` 
*(Двоеточие разделяет слова, точка с запятой — обязательна в конце).*

*   *Пример 1:* `Я вижу, ты очень $Gсильный:сильная;!`
*   *Пример 2:* `Ты $Gготов:готова; к испытанию, $Gгерой:героиня;?`

### 3. Склонение по падежам (Только для русской локализации)
Используется конструкция **`|3-X(слово)`**, где `X` — это номер падежа. 

**Технические особенности русского клиента:**
Из-за того, что базовые макросы (`$C`, `$R`) всегда возвращают слова с Заглавной буквы (даже при использовании строчного `$c`), мыы используем систему падежей для форматирования регистра.

**Единственное число (Работает для классов `$C` и рас `$R`):**
Для базовых слов в клиенте (в файлах `.dbc`) жестко прописаны 6 форм:
*   `|3-1($C)` — Родительный (нет кого? чего?) ➔ *воина*
*   `|3-2($C)` — Дательный (дать кому? чему?) ➔ *воину*
*   `|3-3($C)` — Винительный (вижу кого? что?) ➔ *воина*
*   `|3-4($C)` — Творительный (горжусь кем? чем?) ➔ *воином*
*   `|3-5($C)` — Предложный (думаю о ком? о чем?) ➔ *воине*
*   `|3-6($C)` — **Именительный (Строчной)** ➔ *воин* (Специальный хак для вставки класса с маленькой буквы в середину предложения).

**Множественное число:**
Доступно только для числительных интерфейса (названия классов и рас), так как они зашиты в базу `DeclinedWord.dbc`. Для имен игроков (`$N`) множественное число не поддерживается.
*   `|3-7($C)` — Именительный мн.ч. (кто? что?) ➔ *маги / воины*
*   `|3-8($C)` — Родительный мн.ч. (нет кого? чего?) ➔ *магов / воинов*

*Пример использования:* 
`Приветствую тебя, $c!` ➔ *Приветствую тебя, Воин!* (Заглавный регистр).
`Приветствую тебя, |3-6($c)!` ➔ *Приветствую тебя, воин!* (Идеальный вариант).
`Я передам меч другому |3-2($c).` ➔ *Я передам меч другому воину.*

### 4. Форматирование текста (Переносы и абзацы)
*   **`$B`** или **`$b`** — Перенос строки (Break / Enter). Используется в текстах квестов. Для создания абзаца (пустой строки) пишется дважды: `$B$B`.
*   **`|n`** — Альтернативный перенос строки (используется в Gossip-меню и некоторых системных сообщениях, где `$B` не срабатывает).

### 5. Цвета текста (UI Color Codes)
Позволяет раскрасить любую часть текста.
**Синтаксис:** `|cAARRGGBBТекст|r`
Где `c` — начало цвета, `AA` — альфа-канал (непрозрачность, обычно `ff`), `RRGGBB` — HEX-код цвета, `|r` — сброс цвета (возврат к стандартному).

*Популярные цвета WoW:*
*   `|cffff0000ТЕКСТ|r` — **Красный** (Опасность, враги)
*   `|cff00ff00ТЕКСТ|r` — **Зеленый** (Необычный предмет, успех)
*   `|cff0070ddТЕКСТ|r` — **Синий** (Редкий предмет)
*   `|cffa335eeТЕКСТ|r` — **Фиолетовый** (Эпический предмет)
*   `|cffff8000ТЕКСТ|r` — **Оранжевый** (Легендарный предмет)
*   `|cffffff00ТЕКСТ|r` — **Желтый** (Системные сообщения)

### 6. Интерактивные ссылки (Hyperlinks)
Позволяет вставлять в квесты, письма или диалоги кликабельные ссылки на предметы, квесты или заклинания.

**Ссылка на предмет:**
`|cff1eff00|Hitem:ID_ПРЕДМЕТА:0:0:0:0:0:0:0:0|h[Текст ссылки]|h|r`
*   *Пример:* `Принеси мне |cff1eff00|Hitem:2589:0:0:0:0:0:0:0:0|h[Льняную ткань]|h|r.` (Будет кликабельная зеленая ссылка, при наведении покажет тултип предмета).

**Ссылка на квест:**
`|Hquest:ID_КВЕСТА:УРОВЕНЬ|h[Название квеста]|h`

### 7. Иконки в тексте (UI Textures)
Можно вставлять иконки предметов или заклинаний прямо внутрь текста (очень красиво смотрится в Gossip-диалогах NPC). 
*Внимание: поддерживается не всеми элементами интерфейса (в обычных квестах может не отображаться, отлично работает в Tooltips).*
**Синтаксис:** `|TПуть_К_Текстуре:Размер_X:Размер_Y|t`
*   *Пример:* `|TInterface\Icons\inv_sword_04:16:16|t Я готов выковать тебе меч.` 
*(Перед текстом появится маленькая иконка меча размером 16x16 пикселей).*